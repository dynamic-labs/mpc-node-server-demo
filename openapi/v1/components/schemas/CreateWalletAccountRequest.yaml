type: object
required:
  - roomId
  - clientKeygenIds
  - thresholdSignatureScheme
  - serverEacs
properties:
  serverEacs:
    type: array
    items:
      $ref: ./PartialEAC.yaml
  roomId:
    type: string
  clientKeygenIds:
    type: array
    items:
      type: string
  thresholdSignatureScheme:
     $ref: ./ThresholdSignatureScheme.yaml
