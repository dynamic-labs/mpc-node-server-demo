type: object
required:
  - chain
  - thresholdSignatureScheme
properties:
  chain:
    $ref: ./Chain.yaml
  thresholdSignatureScheme:
    $ref: ./ThresholdSignatureScheme.yaml
  serverEacs:
    type: array
    items:
      $ref: ./EAC.yaml
  parties:
    type: string
    enum:
      - threshold
      - full
