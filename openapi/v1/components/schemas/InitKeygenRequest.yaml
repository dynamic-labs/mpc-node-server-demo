type: object
required:
  - chain
  - environmentId
  - userId
  - thresholdSignatureScheme
properties:
  chain:
    $ref: ./Chain.yaml
  environmentId:
    $ref: ./UUID.yaml
  userId:
    $ref: ./UUID.yaml
  thresholdSignatureScheme:
    $ref: ./ThresholdSignatureScheme.yaml
