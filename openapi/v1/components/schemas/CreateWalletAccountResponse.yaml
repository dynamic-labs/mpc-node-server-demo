type: object
required:
  - eac
  - userId
  - environmentId
  - accountAddress
  - uncompressedPublicKey
  - serverKeyShares

properties:
  userId:
    $ref: ./UUID.yaml
  environmentId:
    $ref: ./UUID.yaml
  accountAddress:
    type: string
  uncompressedPublicKey:
    type: string
  compressedPublicKey:
    type: string
  serverKeyShares:
    type: array
    items:
      $ref: ./ServerKeyShare.yaml
